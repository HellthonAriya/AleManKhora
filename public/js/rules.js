/* =========================================================================
   اَلِ من خورا — Game rules (shared modal)
   Plain, beginner-friendly Persian explanations for every game. Kept in sync
   with the actual engine rules. Call openRules(gameType) from anywhere.
   ========================================================================= */
import { h, modal } from './core.js';

const GAME_TITLES = {
  quoridor: '📖 قوانین «اَلِ من خورا»',
  chess: '📖 قوانین شطرنج',
  chess4: '📖 قوانین شطرنج ۴ نفره',
  chesszade: '📖 قوانین شطرنج زاده‌ای',
  hokm: '📖 قوانین حکم',
  pasur: '📖 قوانین پاسور (چهاربرگ)',
  backgammon: '📖 قوانین تخته‌نرد',
  othello: '📖 قوانین اوتلو',
  gomoku: '📖 قوانین گوموکو',
  dots: '📖 قوانین نقطه‌خط',
  tictactoe: '📖 قوانین دوز',
};

/* ------------------------------ building blocks --------------------------- */
function sec(title, ...nodes) {
  return h('div', { class: 'rules-section' }, h('h3', { class: 'rules-h' }, title), ...nodes);
}
function p(...kids) { return h('p', { class: 'rules-p' }, ...kids); }
function ul(items) { return h('ul', { class: 'rules-ul' }, ...items.map((it) => h('li', {}, it))); }
function piece(glyph, name, how) {
  return h('div', { class: 'rule-piece' },
    h('span', { class: 'rp-glyph' }, glyph),
    h('div', {}, h('b', {}, name), h('div', { class: 'rp-how' }, how)));
}
function tip(text) { return h('div', { class: 'rules-tip' }, '💡 ', text); }

/* --------------------------------- Quoridor ------------------------------- */
function quoridorRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('برنده کسی است که مهرهٔ خودش را اول از همه به ', h('b', {}, 'آن‌سوی تخته'), ' (لبهٔ روبه‌رو) برساند.')),
    sec('🧩 تخته و شروع',
      p('تخته شبکه‌ای از خانه‌هاست. هر بازیکن ', h('b', {}, 'یک مهره'), ' دارد که از وسط یکی از ضلع‌ها شروع می‌کند و باید به ضلع مقابل برسد.')),
    sec('🕹 در نوبت خودت یکی از این دو کار را بکن',
      ul([
        h('span', {}, h('b', {}, '۱) مهره‌ات را حرکت بده'), ' — یک خانه به بالا، پایین، چپ یا راست (هیچ‌وقت مورب).'),
        h('span', {}, h('b', {}, '۲) یک دیوار بگذار'), ' — تا راه حریف را سد کنی و او را دور بزنی.'),
      ])),
    sec('🚶 حرکت مهره',
      ul([
        'فقط به یک خانهٔ کناری می‌روی. از روی دیوار نمی‌توانی رد شوی.',
        'اگر مهرهٔ حریف درست روبه‌روی تو و چسبیده باشد، می‌توانی ',
        h('span', {}, h('b', {}, 'از رویش بپری'), ' و به خانهٔ پشت او بروی.'),
        'اگر پشتِ حریف دیوار یا لبهٔ تخته باشد، به‌جای پریدن می‌توانی ',
        h('span', {}, h('b', {}, 'به‌صورت مورب'), ' از کنار او رد شوی.'),
      ])),
    sec('🧱 گذاشتن دیوار',
      ul([
        h('span', {}, 'هر دیوار ', h('b', {}, 'دو خانه'), ' طول دارد و می‌تواند افقی یا عمودی باشد.'),
        h('span', {}, 'تعداد دیوارهای هرکس ', h('b', {}, 'محدود'), ' است؛ وقتی تمام شد فقط می‌توانی حرکت کنی.'),
        h('span', {}, h('b', {}, 'قانون طلایی:'), ' هرگز نمی‌توانی راه حریف به هدفش را ', h('b', {}, 'کاملاً'), ' ببندی — همیشه باید دست‌کم یک مسیر برایش باز بماند.'),
        'دو دیوار روی هم یا متقاطع نمی‌نشینند.',
      ]),
      tip('برای گذاشتن دیوار، از پنل پایین صفحه دیوار افقی یا عمودی را بردار، روی تخته بکش و رها کن.')),
    sec('🏆 بردن',
      p('هرکس زودتر به لبهٔ هدفش برسد ', h('b', {}, 'برنده'), ' است.')),
    sec('👥 حالت ۴ نفره',
      ul([
        'چهار بازیکن از چهار ضلع شروع می‌کنند و هرکس باید به ضلع روبه‌رو برسد.',
        'تعداد دیوارها کمتر است.',
        'اگر کسی زمانش تمام شود یا خیلی بی‌حرکت بماند، حذف می‌شود و بقیه ادامه می‌دهند.',
      ])),
    sec('🧠 نکته‌های برنده‌شدن',
      ul([
        'دیوار را برای ', h('span', {}, h('b', {}, 'کند کردن حریف'), ' بگذار، نه فقط محض دفاع.'),
        'همیشه حواست به مسیر خودت هم باشد؛ خودت را پشت دیوارها گیر ننداز.',
        'گاهی یک حرکت سادهٔ جلو بهتر از یک دیوار است — عجله در دیوار گذاشتن نکن.',
      ])),
  );
}

/* ---------------------------------- Chess --------------------------------- */
function chessPieces() {
  return h('div', { class: 'rule-pieces' },
    piece('♚', 'شاه', 'یک خانه به هر طرف (افقی، عمودی یا مورب). مهم‌ترین مهره؛ نباید مات شود.'),
    piece('♛', 'وزیر', 'هر تعداد خانه در خط راست یا مورب. قوی‌ترین مهره.'),
    piece('♜', 'رخ (قلعه)', 'هر تعداد خانه افقی یا عمودی.'),
    piece('♝', 'فیل', 'هر تعداد خانه فقط به‌صورت مورب.'),
    piece('♞', 'اسب', 'به شکل حرف L: دو خانه در یک جهت و یک خانه عمود بر آن. تنها مهره‌ای که از روی بقیه می‌پرد.'),
    piece('♟', 'سرباز', 'یک خانه به جلو. در اولین حرکتش می‌تواند دو خانه برود. حریف را فقط به‌صورت مورب (یک خانه جلو‑کنار) می‌زند.'),
  );
}
function chessRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('باید شاه حریف را ', h('b', {}, '«مات»'), ' کنی: طوری تهدیدش کنی که هیچ راه فراری نداشته باشد.')),
    sec('🧩 شروع',
      p('تخته ۸×۸ است. تو مهره‌های پایین را داری و ', h('b', {}, 'اول حرکت می‌کنی'), '؛ حریف بالاست.')),
    sec('♟ هر مهره چطور حرکت می‌کند', chessPieces()),
    sec('⚔️ گرفتن مهره',
      p('برای حذف مهرهٔ حریف، مهرهٔ خودت را روی خانهٔ او ببر (سرباز استثناست: فقط مورب می‌زند). نمی‌توانی روی مهرهٔ خودت بروی.')),
    sec('✨ حرکت‌های ویژه',
      ul([
        h('span', {}, h('b', {}, 'قلعه‌رفتن:'), ' اگر شاه و رخ هیچ‌کدام تا حالا حرکت نکرده‌اند، بین‌شان خالی است، شاه کیش نیست و از خانهٔ تهدیدشده رد نمی‌شود — شاه دو خانه به سمت رخ می‌رود و رخ از کنارش رد می‌شود. برای امن کردن شاه عالی است.'),
        h('span', {}, h('b', {}, 'آن‌پاسان:'), ' اگر سرباز حریف با حرکت دوخانه‌ای دقیقاً از کنار سرباز تو رد شود، فقط در همان نوبت می‌توانی انگار یک خانه رفته باشد، آن را مورب بزنی.'),
        h('span', {}, h('b', {}, 'ارتقا:'), ' اگر سربازت به آخرین ردیف برسد، به وزیر، رخ، فیل یا اسب تبدیل می‌شود (معمولاً وزیر چون قوی‌ترین است).'),
      ])),
    sec('⚠️ کیش، مات و پات',
      ul([
        h('span', {}, h('b', {}, 'کیش:'), ' شاهت تهدید شده. باید فوراً جواب بدهی: مهرهٔ مهاجم را بزن، جلویش مانع بگذار، یا شاه را به خانهٔ امن ببر.'),
        h('span', {}, h('b', {}, 'مات:'), ' شاه کیش است و هیچ راه نجاتی ندارد → بازی تمام و آن طرف برنده است.'),
        h('span', {}, h('b', {}, 'پات (مساوی):'), ' نوبت توست اما هیچ حرکت قانونی نداری و شاهت هم کیش نیست → بازی مساوی می‌شود.'),
      ])),
    sec('🤝 حالت‌های مساوی',
      ul([
        h('span', {}, h('b', {}, 'تکرار سه‌باره:'), ' اگر یک وضعیت سه بار تکرار شود.'),
        h('span', {}, h('b', {}, 'قانون ۵۰ حرکت:'), ' اگر ۵۰ حرکت پشت‌سرهم بدون زدن مهره و بدون حرکت سرباز انجام شود.'),
        h('span', {}, h('b', {}, 'مهرهٔ ناکافی:'), ' وقتی هیچ‌کس به‌اندازهٔ کافی مهره برای مات ندارد (مثلاً فقط دو شاه).'),
        h('span', {}, h('b', {}, 'توافقی:'), ' می‌توانی به حریف پیشنهاد مساوی بدهی؛ اگر بپذیرد بازی مساوی می‌شود.'),
      ])),
    sec('🕹 چطور حرکت کنم؟',
      p('روی مهرهٔ خودت بزن؛ خانه‌های مجاز با ', h('b', {}, 'نقطهٔ سبز'), ' نشان داده می‌شوند؛ بعد روی خانهٔ مقصد بزن. اگر سرباز به ارتقا برسد، یک منوی کوچک برای انتخاب مهرهٔ جدید باز می‌شود.')),
    sec('🧠 نکته‌های شروع',
      ul([
        'اول مهره‌های وسط، اسب و فیل را فعال کن.',
        'هرچه زودتر با قلعه‌رفتن شاهت را امن کن.',
        'مفت مهره نده؛ قبل از هر حرکت ببین مهره‌ات کجا تهدید می‌شود.',
      ])),
  );
}

/* ------------------------------- 4-player chess --------------------------- */
function chess4Rules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('در این بازی چهار ارتش می‌جنگند. هدف این است که ', h('b', {}, 'آخرین بازیکن (یا تیم) باقی‌مانده'), ' باشی.')),
    sec('🧩 تخته و رنگ‌ها',
      p('تخته ', h('b', {}, 'صلیبی‌شکل'), ' است (۱۴×۱۴ که چهار گوشه‌اش حذف شده). چهار ارتش از چهار طرف:'),
      ul([
        h('span', {}, h('b', { style: 'color:#e7503a' }, 'قرمز'), ' پایین (رو به بالا حرکت می‌کند).'),
        h('span', {}, h('b', { style: 'color:#3d7fe0' }, 'آبی'), ' چپ (رو به راست).'),
        h('span', {}, h('b', { style: 'color:#e8b730' }, 'زرد'), ' بالا (رو به پایین).'),
        h('span', {}, h('b', { style: 'color:#3bb15f' }, 'سبز'), ' راست (رو به چپ).'),
      ]),
      p('سربازهای هر بازیکن به سمت مرکز تخته پیش می‌روند. نوبت‌ها ', h('b', {}, 'ساعت‌گرد'), ' جلو می‌رود.')),
    sec('♟ حرکت مهره‌ها',
      p('شاه، وزیر، رخ، فیل، اسب و سرباز ', h('b', {}, 'دقیقاً مثل شطرنج معمولی'), ' حرکت می‌کنند. برای سادگی، در حالت ۴ نفره ', h('b', {}, 'قلعه‌رفتن و آن‌پاسان نداریم'), '.'),
      p(h('b', {}, 'ارتقا:'), ' وقتی سربازت به لبهٔ مقابل (انتهای مسیرش) برسد، به وزیر تبدیل می‌شود.')),
    sec('🆚 دو حالت بازی',
      ul([
        h('span', {}, h('b', {}, 'انفرادی (هرکس برای خودش):'), ' هرکس تنها بازی می‌کند. اگر کسی مات شود یا زمانش تمام شود حذف می‌شود و همهٔ مهره‌هایش از تخته برداشته می‌شود. آخرین کسی که شاهش بماند برنده است.'),
        h('span', {}, h('b', {}, 'تیمی ۲ در ۲:'), ' قرمز و زرد یک تیم، آبی و سبز تیم دیگر (روبه‌روها هم‌تیم‌اند). نمی‌توانی مهرهٔ هم‌تیمی‌ات را بزنی. وقتی هر دو نفرِ یک تیم حذف شوند، تیم دیگر می‌برد.'),
      ])),
    sec('⚠️ کیش، مات و حذف',
      ul([
        'کیش و مات مثل شطرنج معمولی است.',
        'اگر نوبتت شد و شاهت کیش بود و هیچ راه نجاتی نداشتی، ',
        h('span', {}, h('b', {}, 'حذف'), ' می‌شوی (در حالت تیمی، هم‌تیمی‌ات بازی را ادامه می‌دهد).'),
        'اگر زمانت تمام شود یا خیلی بی‌حرکت بمانی هم حذف می‌شوی.',
      ])),
    sec('🧠 نکته‌ها',
      ul([
        'مواظب باش هم‌زمان از چند طرف تهدید نشوی.',
        'گاهی بهتر است بگذاری دو حریف دیگر به جان هم بیفتند و تو صبر کنی.',
        'در حالت تیمی، با هم‌تیمی‌ات هماهنگ حمله کن.',
      ])),
  );
}

/* -------------------------------- Backgammon ------------------------------ */
function backgammonRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('اول باید همهٔ ', h('b', {}, '۱۵ مهره'), 'ات را به خانهٔ خودت (یک‌چهارم تخته) بیاوری و بعد همه را از تخته ', h('b', {}, 'خارج'), ' کنی. هرکس زودتر همهٔ مهره‌هایش را خارج کند برنده است.')),
    sec('🎲 ریختن تاس و حرکت',
      ul([
        'در هر نوبت دو تاس برایت ریخته می‌شود (به‌صورت خودکار).',
        h('span', {}, 'هر تاس یعنی چند خانه یک مهره را ', h('b', {}, 'جلو'), ' ببری. می‌توانی دو تاس را روی یک مهره یا دو مهرهٔ جدا بازی کنی.'),
        h('span', {}, 'اگر ', h('b', {}, 'جفت'), ' بیاوری (مثلاً ۵-۵)، چهار بار آن عدد را بازی می‌کنی.'),
        'اگر هیچ حرکت قانونی نداشته باشی، نوبت خودکار رد می‌شود.',
      ])),
    sec('🛡 خانه‌ها و زدن مهره',
      ul([
        h('span', {}, 'اگر روی یک خانه ', h('b', {}, 'دو مهره یا بیشتر'), ' از حریف باشد، نمی‌توانی آنجا بروی.'),
        h('span', {}, 'اگر روی خانه ', h('b', {}, 'فقط یک'), ' مهرهٔ حریف باشد (تک‌مهره)، می‌توانی آن را ', h('b', {}, 'بزنی'), '؛ آن مهره به وسط تخته (میله) می‌رود.'),
        h('span', {}, 'تا وقتی مهره‌ای روی ', h('b', {}, 'میله'), ' داری، اول باید آن را از خانهٔ شروعِ حریف وارد تخته کنی؛ تا آن نشود حق حرکت دیگری نداری.'),
      ])),
    sec('🏁 خارج کردن مهره‌ها',
      ul([
        'وقتی هر ۱۵ مهره‌ات به خانهٔ خودت رسید، می‌توانی با تاس آن‌ها را از تخته خارج کنی.',
        'برای خارج کردن، عدد تاس باید دقیقاً با فاصلهٔ مهره تا بیرون جور باشد (یا با شرایط استاندارد، تاس بزرگ‌تر).',
      ])),
    sec('🕹 چطور بازی کنم؟',
      p('اول روی مهرهٔ خودت بزن؛ خانه‌های مجاز روشن می‌شوند؛ بعد روی خانهٔ مقصد بزن. تاس‌ها خودکار ریخته می‌شوند و وقتی تمام شدند، نوبت به حریف می‌رسد.')),
    tip('مهره‌های تک‌و‌تنها را کم بگذار تا حریف نتواند بزندشان؛ خانه‌سازی پشت‌سرهم راه او را می‌بندد.'),
  );
}

/* --------------------------------- Othello -------------------------------- */
function othelloRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('وقتی تخته پر شد (یا کسی نتواند حرکت کند)، هرکس ', h('b', {}, 'مهرهٔ بیشتری'), ' روی تخته داشته باشد برنده است.')),
    sec('🧩 شروع',
      p('تخته ۸×۸ است و در وسط چهار مهره (دوتا از هر رنگ) چیده شده. بازیکن تیره اول بازی می‌کند.')),
    sec('🔄 حرکت و برگرداندن',
      ul([
        h('span', {}, 'یک مهره جایی می‌گذاری که ', h('b', {}, 'حداقل یک ردیف'), ' از مهره‌های حریف را بین مهرهٔ تازه‌ات و یکی از مهره‌های قبلی‌ات ', h('b', {}, 'محاصره'), ' کند (افقی، عمودی یا مورب).'),
        h('span', {}, 'همهٔ مهره‌های محاصره‌شده ', h('b', {}, 'به رنگ تو برمی‌گردند'), '.'),
        'فقط جاهایی می‌توانی بگذاری که حداقل یک مهره برگردانده شود؛ این خانه‌ها برایت نشان داده می‌شوند.',
        'اگر هیچ حرکت قانونی نداشتی، نوبتت رد می‌شود.',
      ])),
    sec('🧠 نکته‌ها',
      ul([
        h('span', {}, h('b', {}, 'گوشه‌ها'), ' خیلی ارزشمندند چون دیگر برنمی‌گردند؛ سعی کن آن‌ها را بگیری.'),
        'تعداد زیاد مهره در وسط بازی مهم نیست؛ مهم وضعیت پایان است.',
        'مراقب باش با حرکتت به حریف راه گرفتن گوشه را ندهی.',
      ])),
  );
}

/* --------------------------------- Gomoku --------------------------------- */
function gomokuRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('اولین کسی که ', h('b', {}, 'پنج مهره'), ' از خودش را پشت‌سرهم در یک خط (افقی، عمودی یا مورب) بچیند، ', h('b', {}, 'برنده'), ' است.')),
    sec('🕹 نوبت‌ها',
      ul([
        'به نوبت، هرکس یک مهره روی یک تقاطع خالی می‌گذارد.',
        'مهره‌ها بعد از گذاشتن جابه‌جا نمی‌شوند.',
        'بازیکن سیاه (تو) معمولاً اول شروع می‌کند.',
      ])),
    sec('🧠 نکته‌ها',
      ul([
        h('span', {}, 'هم به ', h('b', {}, 'حملهٔ'), ' خودت فکر کن هم جلوی ', h('b', {}, 'چهارتاییِ'), ' حریف را بگیر.'),
        'یک «چهارتای باز» (دو سرش خالی) تقریباً برد است؛ نگذار حریف بسازد.',
        'گاهی یک حرکت هم‌زمان حمله و دفاع است — دنبال آن باش.',
      ])),
  );
}

/* ------------------------------- Dots & Boxes ----------------------------- */
function dotsRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('با کشیدن خط بین نقطه‌ها مربع بساز. هرکس ', h('b', {}, 'مربع‌های بیشتری'), ' ببندد برنده است.')),
    sec('🕹 نوبت‌ها',
      ul([
        h('span', {}, 'در نوبتت، یک ', h('b', {}, 'خط'), ' بین دو نقطهٔ کنار هم می‌کشی (افقی یا عمودی).'),
        h('span', {}, 'اگر با خطی که کشیدی یک مربع ', h('b', {}, 'کامل'), ' شود (هر چهار ضلعش بسته شود)، آن مربع مال توست و ', h('b', {}, 'دوباره نوبت توست'), '.'),
        'اگر مربعی کامل نکنی، نوبت به حریف می‌رسد.',
      ])),
    sec('🧠 نکته‌ها',
      ul([
        h('span', {}, 'تا می‌توانی ضلع ', h('b', {}, 'سومِ'), ' یک مربع را نکش، چون حریف ضلع چهارم را می‌بندد و مربع را می‌برد.'),
        'وقتی مجبور شدی، کوتاه‌ترین «زنجیره» را به حریف بده تا کمترین مربع را از دست بدهی.',
        'بازیکن‌های حرفه‌ای با تکنیک «دو فدا کردن» کنترل زنجیره‌های بزرگ را نگه می‌دارند.',
      ])),
  );
}

/* ---------------------------------- Doz ----------------------------------- */
function tictactoeRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('اولین کسی که ', h('b', {}, 'سه‌تا'), ' علامت خودش را در یک خط (افقی، عمودی یا مورب) بچیند برنده است.')),
    sec('🕹 نوبت‌ها',
      ul([
        'تخته ۳×۳ است. تو ✕ هستی و حریف ◯ (یا برعکس).',
        'به نوبت، هرکس روی یک خانهٔ خالی علامتش را می‌گذارد.',
        'اگر تخته پر شود و کسی سه‌تایی نکند، بازی ', h('b', {}, 'مساوی'), ' است.',
      ])),
    tip('خانهٔ وسط ارزشمندترین است؛ و همیشه حواست به سه‌تاییِ نزدیکِ حریف باشد تا جلویش را بگیری.'),
  );
}

/* ------------------------------ شطرنج زاده‌ای ----------------------------- */
function chesszadeRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('مثل شطرنج معمولی، باید شاه حریف را ', h('b', {}, 'مات'), ' کنی. تنها فرقش ', h('b', {}, 'چیدمان اولیهٔ'), ' مهره‌هاست.')),
    sec('🔀 چیدمان تصادفی',
      ul([
        h('span', {}, 'در شروع هر بازی، مهره‌های ', h('b', {}, 'ردیف عقب'), ' (رخ، اسب، فیل، وزیر، شاه) به‌صورت ', h('b', {}, 'تصادفی'), ' چیده می‌شوند. سربازها سر جای همیشگی‌شان می‌مانند.'),
        h('span', {}, 'حالت ', h('b', {}, 'سربازها هم تصادفی'), ': اگر روشن باشد، هر دو ردیف (سربازها و مهره‌های عقب) کاملاً درهم و تصادفی چیده می‌شوند.'),
        h('span', {}, 'حالت ', h('b', {}, 'آینه‌ای'), ' (پیش‌فرض): چیدمان تو و حریف ', h('b', {}, 'یکسان'), ' است (هر مهره روبه‌روی هم‌نوع خودش). در حالت ', h('b', {}, 'نامتقارن'), ' هر طرف چیدمان تصادفیِ جداگانه دارد.'),
      ])),
    sec('♟ حرکت‌ها',
      p('همهٔ مهره‌ها ', h('b', {}, 'دقیقاً مثل شطرنج معمولی'), ' حرکت می‌کنند و می‌زنند. ارتقای سرباز هم مثل قبل است.'),
      p('چون ردیف عقب به‌هم ریخته است، ', h('b', {}, 'قلعه‌رفتن'), ' در این حالت وجود ندارد.')),
    sec('🧠 نکته',
      p('چون چیدمان آشنا نیست، چند حرکت اول را با دقت بازی کن: ببین مهره‌هایت کجا را پوشش می‌دهند و شاهت چقدر امن است.')),
  );
}

/* ---------------------------------- Hokm ---------------------------------- */
function hokmRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('با برنده شدن در «دست»‌ها (هر دور بازیِ ورق‌ها) امتیاز بگیر. هرکس (یا هر تیمی) زودتر به تعداد دست لازم برسد، ', h('b', {}, 'برنده'), ' است.')),
    sec('👥 تعداد بازیکن',
      ul([
        h('span', {}, h('b', {}, '۴ نفره (تیمی):'), ' دو تیمِ دو نفره؛ روبه‌روها هم‌تیم‌اند. هر نفر ۱۳ ورق. برای برد باید ', h('b', {}, '۷ دست'), ' ببرید.'),
        h('span', {}, h('b', {}, '۳ نفره:'), ' هرکس برای خودش، هر نفر ۱۷ ورق (یک ورق از دسته کم می‌شود). برندهٔ کسی است که زودتر ', h('b', {}, '۹ دست'), ' ببرد.'),
        h('span', {}, h('b', {}, '۲ نفره:'), ' هرکس ۲۶ ورق؛ هرکس زودتر ', h('b', {}, '۱۴ دست'), ' ببرد می‌برد.'),
      ])),
    sec('👑 حاکم و حُکم',
      p('یک نفر «حاکم» است. او ', h('b', {}, 'خالِ حکم'), ' (خال برنده) را انتخاب می‌کند؛ آن خال در تمام دست‌ها از بقیه قوی‌تر است. حاکم اولین دست را شروع می‌کند.')),
    sec('🃏 بازی هر دست',
      ul([
        'نفر شروع‌کننده یک ورق می‌گذارد؛ خالِ آن ورق «خالِ زمین» می‌شود.',
        h('span', {}, h('b', {}, 'قانون طلایی:'), ' اگر از خالِ زمین ورق داری، ', h('b', {}, 'باید'), ' همان خال را بازی کنی.'),
        'اگر از خالِ زمین نداری، هر ورقی می‌توانی بازی کنی — از جمله ورقِ حُکم برای بردن دست.',
        h('span', {}, 'بالاترین ورقِ ', h('b', {}, 'حُکم'), ' دست را می‌برد؛ اگر حُکمی بازی نشده باشد، بالاترین ورقِ ', h('b', {}, 'خالِ زمین'), ' برنده است.'),
        'برندهٔ هر دست، دستِ بعدی را شروع می‌کند.',
      ])),
    sec('🕹 چطور بازی کنم؟',
      p('اگر حاکمی، اول از منوی وسط، خالِ حکم را بزن. بعد در نوبتت، روی ورقی که می‌خواهی بازی کنی بزن (ورق‌های مجاز برجسته‌اند).')),
    sec('🧠 نکته‌ها',
      ul([
        'ورق‌های حُکم را برای بردن دست‌های مهم نگه دار.',
        'در حالت تیمی، اگر هم‌تیمی‌ات دارد دست را می‌برد، ورق بزرگت را حرام نکن.',
        'آس و شاهِ خال‌های غیرحُکم را به‌موقع بازی کن تا از بین نروند.',
      ])),
  );
}

function pasurRules() {
  return h('div', { class: 'rules-body' },
    sec('🎯 هدف بازی',
      p('بازیِ ورقِ ایرانی برای ۲ نفر — به آن ', h('b', {}, 'چهاربرگ'), ' هم می‌گویند. با «شکار» کردن ورق‌های روی میز، برگ و امتیاز جمع کن؛ در چند دستِ پیاپی، اولین کسی که به ', h('b', {}, '۶۲ امتیاز'), ' برسد برندهٔ مسابقه است.')),
    sec('🃏 شروع',
      ul([
        'به هر بازیکن ۴ ورق و روی میز هم ۴ ورق رو باز می‌شود.',
        'وقتی دستِ هر دو تمام شد، دوباره ۴ ورق به هرکس داده می‌شود تا دسته تمام شود.',
      ])),
    sec('✨ عددِ جادویی: ۱۱',
      ul([
        h('span', {}, h('b', {}, 'ورق عددی (آس تا ۱۰):'), ' آس=۱. ورقی که می‌گذاری اگر با یک یا چند ورقِ عددیِ روی میز ', h('b', {}, 'جمعاً ۱۱'), ' شود، همهٔ آن‌ها را برمی‌داری. مثلاً ۷ یک ۴ را می‌گیرد، یا ۳ و آس را.'),
        h('span', {}, h('b', {}, 'برداشت اجباری است:'), ' اگر ورقی که می‌زنی بتواند چیزی بردارد، حتماً باید برداری و نمی‌توانی آن را روی میز بگذاری. اگر فقط یک راهِ برداشت باشد، خودکار انجام می‌شود.'),
        h('span', {}, h('b', {}, 'سرباز (J):'), ' همهٔ ورق‌های روی میز را جمع می‌کند — به‌جز بی‌بی و شاه.'),
        h('span', {}, h('b', {}, 'بی‌بی و شاه (Q/K):'), ' فقط ورقِ هم‌نام خود را از میز برمی‌دارند.'),
        'فقط ورقی که هیچ برداشتی ندارد روی میز گذاشته می‌شود.',
      ])),
    sec('🧹 سور',
      ul([
        h('span', {}, 'اگر با یک حرکت ', h('b', {}, 'همهٔ'), ' ورق‌های روی میز را جمع کنی و میز خالی شود، یک ', h('b', {}, 'سور'), ' گرفته‌ای (۵ امتیاز).'),
        h('span', {}, 'با ', h('b', {}, 'سرباز'), ' سور حساب نمی‌شود، حتی اگر همهٔ میز را جمع کند.'),
      ])),
    sec('🏁 امتیاز هر دست',
      ul([
        'با تمام‌شدن ورق‌ها، آخرین کسی که برگ گرفته، باقیماندهٔ میز را برمی‌دارد.',
        'هر آس: ۱ — هر سرباز: ۱',
        '۱۰ خشت (دل‌قرمز): ۳ — ۲ گشنیز (خاج): ۲',
        'هر سور: ۵',
        h('span', {}, h('b', {}, 'بیشترین برگ'), ' (بیش از ۲۶): ۷ — ', h('b', {}, 'بیشترین خاج ♣'), ' (۷ یا بیشتر): ۷'),
      ])),
    sec('🏆 برندهٔ مسابقه',
      p('بازی از چند دستِ پیاپی تشکیل می‌شود و امتیازِ هر دست جمع می‌شود. اولین کسی که به ', h('b', {}, '۶۲ امتیاز'), ' برسد، برندهٔ کلِ مسابقه است. پس از هر دست، شمارشِ امتیازها برگ‌به‌برگ نمایش داده می‌شود.')),
    sec('🕹 چطور بازی کنم؟',
      p('روی یک ورق از دستت بزن تا انتخاب شود. با ورق عددی، ورق‌های میز را که با کارتت ۱۱ می‌شوند انتخاب کن (برجسته می‌شوند) و «بازی کن» را بزن. اگر فقط یک راهِ برداشت باشد خودکار انتخاب می‌شود. ورق‌های تصویری خودکار هدف‌گیری می‌کنند.')),
  );
}

const RENDERERS = {
  quoridor: quoridorRules, chess: chessRules, chess4: chess4Rules, chesszade: chesszadeRules,
  hokm: hokmRules, pasur: pasurRules, backgammon: backgammonRules, othello: othelloRules, gomoku: gomokuRules,
  dots: dotsRules, tictactoe: tictactoeRules,
};

/** Open the rules modal for a given game type. */
export function openRules(gameType = 'quoridor') {
  const render = RENDERERS[gameType] || RENDERERS.quoridor;
  modal({
    title: GAME_TITLES[gameType] || GAME_TITLES.quoridor,
    body: render(),
    actions: [{ label: 'فهمیدم!', class: 'btn-primary' }],
  });
}

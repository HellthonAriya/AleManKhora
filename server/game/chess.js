/**
 * AleManKhora — Chess engine (server side).
 * The canonical implementation lives in `public/js/chess.js` and is shared by
 * the server and the browser; this module re-exports it for stable imports.
 */
export * from '../../public/js/chess.js';
